language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.8"

cache:
  - apt

before_install:
  - python -m pip install --upgrade pip

install:
  - pip install -r dev-requirements.txt

# command to run tests
script:
  - pytest bidsphysio
